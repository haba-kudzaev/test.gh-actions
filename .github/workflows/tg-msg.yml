name: tg-msg

on:
  push:
    branches:
      - main
      - dev

jobs:
  send_msg:
    name: Отправка сообщения в Telegram с информацией о коммите
    runs-on: ubuntu-latest
    steps:
      - name: Отправка сообщения в Telegram
        uses: appleboy/telegram-action@master
        with:
          to: ${{ secrets.TELEGRAM_GROUP_ID }}
          format: markdown
          token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          photo: test/github.png
          message: |
            📌 @${{ github.actor }} опубликовал новый [коммит](https://github.com/${{ github.repository }}/commit/${{github.sha}})!